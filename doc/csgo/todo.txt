1) добавить логику для новых оружий
    1) MOLOTOV как HEGRENADE
    2) INCGRENADE как HEGRENADE
    3) TASER как KNIFE (только при использовании нужно, чтобы оружие убиралось с карты и не засоряло)

2) known issues
    1) MOTD does not work
    2) "GameDesc" config variable does not work.
    3) the volume very low on the gungame sounds
    4) error
    ----
L 08/21/2012 - 22:08:00: [SM] Native "GetEntProp" reported: Entity 151 (151) is invalid
L 08/21/2012 - 22:08:00: [SM] Displaying call stack trace for plugin "gungame.smx":
L 08/21/2012 - 22:08:00: [SM]   [0]  Line 1448, E:\Downloads\sm_gungame-1.2.0.0.beta2-csgo\addons\sourcemod\scripting\gungame/util.sp::UTIL_RemoveAmmoNew()
L 08/21/2012 - 22:08:00: [SM]   [1]  Line 62, E:\Downloads\sm_gungame-1.2.0.0.beta2-csgo\addons\sourcemod\scripting\gungame/hacks.sp::HACK_RemoveAmmo()
L 08/21/2012 - 22:08:00: [SM]   [2]  Line 769, E:\Downloads\sm_gungame-1.2.0.0.beta2-csgo\addons\sourcemod\scripting\gungame/util.sp::UTIL_DelayAmmoRemove()  

    Тут используется задержка в 0.1 секунды. За это время оружие может пропасть у игрока? Оружие даётся при ставне или при левелапе.
    А может попробовать сразу у оружия обнулять патроны?
    ----

3>) refactoring - same version for css ans csgo!
    1+) +добавляем int переменную - количество типов оружий, равное количеству оружий в файле weapons.config.txt.
        +добавляем константу - маскимально возможное количество типов оружия
    2+) +добавляем array переменную - связь название оружия -> его индекс. Для этого у нас сейчас trie используется. ок.
    3>) +добавляем array переменную - связь id оружия -> его название. string24 . в файле с оружиями добавить соотсветсвующее свойство
        >меняем все места использования WeaponName
    4>) +добавляем array переменную - связь id оружия -> его слот. int . в файле с оружиями добавить соотсветсвующее свойство
        >меняем все места использования WeaponSlot
    5>) +добавляем array переменную - связь id оружия -> его клипсайз. int . в файле с оружиями добавить соотсветсвующее свойство
        >меняем все места использования WeaponAmmo
    6) выпиливаем все WeaponId типы переменных, меняя их на int.
    7) enum Weapons удаляем, меняем на интерфейсные методы
        1) находим все места, где используются CSW_* типы. Включая Weapons:MAXWEAPON.
        2) для получения ID оружия используем новые методы, типа GetWeaponIdKnife(/hegrenade/flashbang/smokegrenade/kevlar/etc./)
        3) меняем везде эти места на методы-аналоги
        4) для этих методов реализовываем int переменные, в которых хранятся ID оружий из файла настроек
        5) инициализируем их из этого файла настроек, например добавив новые свойства - isHegrenade или isKnife, чтобы не завязыватсья на названия
